<% include partials/header %>
    
        <h1>Enter New Password</h1>
        <form action="\forgotPassword\requestChange\<%= oldHash %>\<%= username%>" method="POST">
            <input type="password" name="password1" placeholder="password" id="password" onkeyup='check();'><br>
            <input type="password" name="password2" placeholder="confirm password" id="confirm_password" onkeyup='check();'>
            <div id="message"></div>
            <button id="submitButton">submit</button>
        </form>
        <a href="\">go back to home</a>
        
        
        
        <script>
            var button = document.getElementById("submitButton");
            var check = function() {
              if (document.getElementById('password').value ==
                document.getElementById('confirm_password').value) {
                button.disabled = false;
                document.getElementById('message').style.color = 'green';
                document.getElementById('message').innerHTML = 'matching';
              } else {
                button.disabled = true;
                document.getElementById('message').style.color = 'red';
                document.getElementById('message').innerHTML = 'not matching';
              }
            }
        </script>
<% include partials/globalSocketIO %>
<% include partials/footer %>